# DNS-Bind9 
#
# VERSION               0.0.1

FROM      ubuntu
LABEL Description="This image has the DNS Server" Vendor="GutiTrombotto" Version="1.0"
RUN apt-get update -y && apt-get -y upgrade && apt-get install bind9 bind9utils bind9-doc -y iputils-ping -y nano -y
